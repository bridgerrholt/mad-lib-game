// Returns a string in lower case using std::transform.
// Not compatible with non-ASCII characters.

#ifndef GET_LOWERED_H
#define GET_LOWERED_H

#include <string>

std::string getLowered(std::string inputString);

#endif